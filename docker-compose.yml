version: '3'
services:
  app:
    container_name: ruby_playground
    build: .
    command: bundle exec rails -s -p 3000 -b '0.0.0.0'
    ports:
      - "3001:3000"
    environment:
      - DB_NAME=""
      - DB_HOST=""
      - DB_USER=""
      - DB_PASSWORD=""
    restart: on-failure
